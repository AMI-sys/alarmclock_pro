package com.example.dindon.ui.theme

import androidx.compose.ui.graphics.Color

object Neu {
    val bg = Color(0xFFF0F2F5)

    val onBg = Color(0xFF1C1B1F)

    val lightShadow = Color.White.copy(alpha = 0.9f)
    val darkShadow = Color.Black.copy(alpha = 0.12f)

    val outline = Color.Black.copy(alpha = 0.04f)

}
